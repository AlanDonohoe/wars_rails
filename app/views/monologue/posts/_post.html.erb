<%= link_to post.full_url do %>
<section class="masonry-grid-item <%= post.tags.map(&:name).join(' ')%>" data-tags="<%= post.tags.map(&:name).join(' ') %>">
  <%#render partial: 'monologue/posts/post_header', locals: {post: post, truncate_length: 17} %>
  <%= first_img_url(post) %>
  <header class="post-header pls">
    <%= truncate(post.title, length: 45 ) %>
  </header>

  <div class="content" data-monologue="content">
    <%# truncate_html(post.content, length: Monologue::Config.preview_size, omission: '...') %>
  </div>

  <!-- <div class="tags" data-monologue="tags">
    <%= render post.tags %>
  </div> -->
  <%# link_to t(".readmore"), post.full_url, class: "button button-outline" %>
</section>
<% end %>
